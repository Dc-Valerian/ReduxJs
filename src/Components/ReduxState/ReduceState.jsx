import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const ReduceState = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = ReduceState.actions

export default ReduceState.reducer